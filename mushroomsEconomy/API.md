# Описание API

Здесь описано всё API, используемое в микросервисе (используется для общения между сервисами), с описанием структур данных.

**Содержание**

1. Общее
   * 1.1. Адрес сервера
   * 1.2. Используемый протокол
2. Структуры данных
   * 2.1. Общий формат ответа
3. Список запросов
   * 3.1. Общие ошибки
4. Подробно
   * 4.1. //Тут будут названия запросов REST
   * 

## 1. Общее

### 1.1. Адрес сервера

`http://localhost:3005`

### 1.2. Используемый протокол

API полностью реализовано на http(s).

Формат возвращаемых значений `JSON`.

Все методы, если это особо не оговорено, имеют тип `GET`.

## 2. Структуры данных

### 2.1. Общий формат ответа

`T` - какие-то данные. В случае успешного ответа возвращается `result = 'ok'` и поле `data` с данными.

В случае ошибки возвращается `result = 'error'` и поле `error` с кодом и текстом ошибки

```
Answer<T>: {
    result: 'ok' | 'error';
    data?: T;
    error?: {
        code: number;
        text: string;
    };
}
```

## 3. Список запросов

| Название | О чем |
| ---------------- | --------- |
|                  |           |

### 3.1. Общие ошибки

* `101` - если не передан параметр `method`
* `102` - если переданное значение в параметре `method` не обрабатывается
* `103` - не переданы все необходимые параметры
* `9000` - неизвестная ошибка

## 4. Подробно

### 4.1. //Пример

Авторизация пользователя в системе

**Параметры**

```
{
    login: string; - логин пользователя
    hash: string; - контрольная сумма, равная hash = md5(md5(login + password) + rnd)
    rnd: number; - случайное целое число
}
```

**Успешный ответ**

```
Answer<User>
```

**Ошибки**

* `1002` - неверный пароль
* `1005` - неверный логин
* `1007` - неправильная длина логина
* `1008` - логин начинается с цифры или подчеркивания
* `1009` - недопустимые символы в логине
* `1010` - логин содержит пробелы или специальные символы
